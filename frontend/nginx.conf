server {
    listen 80;
    server_name adlan-kovka.ru www.adlan-kovka.ru;

    # Перенаправляем всё с HTTP → HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name adlan-kovka.ru www.adlan-kovka.ru;

    # 🔒 SSL-сертификаты от Let's Encrypt
    ssl_certificate     /etc/letsencrypt/live/adlan-kovka.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/adlan-kovka.ru/privkey.pem;

    # Корневая папка фронтенда (React/Vite)
    root /usr/share/nginx/html;
    index index.html;

    # 🎯 Все запросы к фронту — отдаём index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 🚀 Проксируем запросы к backend (Go)
    location /api/ {
        proxy_pass         http://backend:8080/api/;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # 📸 Проксируем загрузку изображений
    location /uploads/ {
        proxy_pass         http://backend:8080/uploads/;
    }
}
